# docker-compose.base.yaml (shared configuration)
services:
  tee:
    stdin_open: true
    tty: true
    volumes:
      - /var/run/tappd.sock:/var/run/tappd.sock
    ports:
      - "3001:80"
    restart: always

volumes:
  tweet-cache: {}
  db-data: {}
